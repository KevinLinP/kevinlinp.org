// This file handles layout data loading
// Here we can define any preload functions if needed

export const prerender = true;  // Enable static site generation
export const ssr = false;        // Disable server-side rendering